# README

Useful commands:

```
systemctl daemon-reload                                # to reload configuration
systemctl status systemd-timesyncd.service             # retrieve the status of a service
systemctl restart systemd-timesyncd.service            # restart a service
systemctl start prometheus                             # start a service
systemctl enable prometheus                            # enable a service on startup
```
